// import {createData} from "../components/ShoppingContainer"

export const addToList = (item: string) => { 
    console.log(item);
    // createData(item);
}

export const RemoveFromList = (item: string) => { 
    console.log(item);
}